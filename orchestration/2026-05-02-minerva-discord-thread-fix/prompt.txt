/ulw You are the OpenCode developer dream team contracted by Jinwang through Boramae. Continue the existing feature branch in this worktree and finish the missing core requirements for the Jarvis-native /minerva /minerva workflow harness.

Context:
- Repo/worktree: /home/jinwang/workspace/jinwang-jarvis-minerva-worktree
- Branch: feature/minerva-contract-runtime
- Existing MVP files include src/jinwang_jarvis/minerva.py, src/jinwang_jarvis/cli.py, tests/test_minerva.py, skills/minerva/SKILL.md, skills/minerva/SKILL.md, docs/minerva-workflow.md.
- Current state works as local CLI artifact recorder but is incomplete.
- Original target: invoking /minerva or /minerva from Discord should auto-open a task-specific Discord thread, use that thread as the operating context, then run Interview -> Seed -> Execute -> Evaluate -> Evolve with status/export/drift artifacts. It must remain Jarvis-owned and avoid Hermes source mutation if possible.

Required deliverables:
1. Implement automatic Discord thread opening support in Jarvis-owned code.
   - It must be testable without live Discord by injecting/mocking a thread client/adapter.
   - Live side effect must be explicit and gated. Do NOT create real Discord threads during tests.
   - Existing CLI start should support a safe mode where it can request thread creation from a pluggable adapter or write a clear pending-thread action if live gateway/tool integration is not available inside plain CLI.
   - Store created thread metadata: platform, parent/channel id, thread id, thread name, message id if available, jump/url if available, created/pending/error state.
   - Update origin/status/export artifacts accordingly.
2. Implement the gateway/handoff boundary enough for MVP.
   - If direct Discord API cannot be safely called from Jarvis CLI without Hermes runtime, design and implement a deterministic handoff artifact/contract that Hermes/Boramae can consume to call discord.create_thread, and provide CLI support for marking the thread as created. But prefer a clean adapter abstraction if possible.
   - Do not modify Hermes source, ~/.hermes, systemd, cron, secrets, or external services.
   - Do not require live credentials for unit tests.
3. Fix phase guard/state machine integrity.
   - Disallow illegal transitions such as start->seed->evolve, evaluate before run, run before seed, etc.
   - CLI should return deterministic nonzero errors for invalid transitions; tests should cover this.
4. Improve tests.
   - Cover auto-thread creation through fake adapter or handoff contract.
   - Cover pending thread behavior if live creation unavailable.
   - Cover minerva alias invocation, invalid phase transitions, and export/status thread metadata.
   - Existing tests must still pass.
5. Update docs/skills/orchestration result.
   - Skills and docs must say the real flow is: Discord thread auto-open -> Interview -> Seed -> Execute -> Evaluate -> Evolve.
   - Write final result to: orchestration/2026-05-02-minerva-discord-thread-fix/result.md
   - Also write machine-readable evidence to: orchestration/2026-05-02-minerva-discord-thread-fix/evidence.json

Verification you must run before claiming done:
- PYTHONPATH=src pytest -q tests/test_minerva.py
- PYTHONPATH=src pytest -q tests/test_minerva.py tests/test_cli.py tests/test_bootstrap.py
- PYTHONPATH=src python -m compileall -q src/jinwang_jarvis tests/test_minerva.py
- A manual isolated temp CLI smoke that starts with thread auto-open fake/handoff, records at least one turn, seed, run, evaluate, evolve, status, export.
- Static scan or grep/AST check that no subprocess/shell/network calls were added unsafely to the Minerva path unless carefully isolated behind an adapter and disabled by default.

Constraints:
- Do not touch the main checkout /home/jinwang/workspace/jinwang-jarvis.
- Do not touch Hermes source or ~/.hermes.
- Do not create real Discord threads during implementation/testing.
- Keep the feature additive and local to Jarvis where possible.
- If you discover the right solution requires later Hermes gateway integration, implement the Jarvis-side contract now and document the exact remaining Hermes-side hook as a gap, not as completed.
- Do not stop after planning. Implement synchronously in this process. Do not wait for background agents without producing the required result.md/evidence.json.
